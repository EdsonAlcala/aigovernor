{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1",
    "exponential-backoff": "^3.1.1",
    "hardhat-deploy": "^0.11.44",
    "openai": "^4.20.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat deploy",
    "deploy:base-goerli": "hardhat deploy --network base_goerli --reset",
    "start:node": "ts-node nodes/index.ts"
  }
}
